#Data Type Conversion - simply use type-names as a f(x)
  repr(x)
  #eval(str1)
  tuple(s)
  list(s)
  set(s)
  #dict(d) #creates a dictionary d must be sequence of (key, value) tuple

#Mathematical Function
  abs(x) #absolute value of x (the positive)
  ceil(x) #ceiling of x: the smallest integer not less than x
  cmp(x, y) #-1 if x<y, 0 if x==y, 1 if x> y (not in python 3.0)
  exp(x) #exponential of x=(e)^x
  fabs(x) #absolute value of x
  floor(x) #largest integer not greater than x
  log(x) #natural log of x, x>0
  log10(x) #base 10 log of x, for x>0
  max(x1, x2, x3, x4....) #largest args: value closest to positive infinity
  min(x1, x2, x3, x4....) #smallest args: value closest to negative infinity
  modf(x) #fractional & integer parts of x in two item tuple/ integer part returns float
  pow(x, y) #the value of x**y
  sqrt(x) #the sqrtX of x for x>0
  rounded(x[n]) #x is rounded to n digits from the decimal point
  
#Trigonometric Function  
  acos(x) #arc consine of x in radians
  asin(x) #arc sin of x in radians
  atan(x) #arc tan of x in radians
  atan2(y, x) #a tan(y/x), in radians
  cos(x) # cosine of x radians
  hypot(x, y) # euclidean norm, sqrt(x*x, y*y)
  sin(x) # sin of x radians
  tan(x) # tangent of x radians
  degrees(x) # convert angle x from radians to degrees
  
#Built in list/tuple functions are
  lens(list) #total length of list
  max(list) #return item from list w/ max value
  min(tuple) #return item from list w/ min value
  list(tuple) #converts tuples into a list
  
#Built in Dict Functions
  lens(dict)
  str(dict)
  type(varible)
  

  












